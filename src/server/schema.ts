import {pgTable,text,timestamp,boolean,integer,jsonb,uuid,primaryKey,index,uniqueIndex} from 'drizzle-orm/pg-core';
import type {Field,Diagnosis,Confirmation,Value,DataRecord} from '../shared/contracts';
const time=(name:string)=>timestamp(name,{withTimezone:true}).notNull().defaultNow();
export const user=pgTable('app_user',{id:text().primaryKey(),name:text().notNull(),email:text().notNull().unique(),emailVerified:boolean().notNull().default(false),image:text(),createdAt:time('created_at'),updatedAt:time('updated_at')});
export const session=pgTable('session',{id:text().primaryKey(),expiresAt:timestamp('expires_at',{withTimezone:true}).notNull(),token:text().notNull().unique(),createdAt:time('created_at'),updatedAt:time('updated_at'),ipAddress:text('ip_address'),userAgent:text('user_agent'),userId:text('user_id').notNull().references(()=>user.id,{onDelete:'cascade'})});
export const account=pgTable('account',{id:text().primaryKey(),accountId:text('account_id').notNull(),providerId:text('provider_id').notNull(),userId:text('user_id').notNull().references(()=>user.id,{onDelete:'cascade'}),accessToken:text('access_token'),refreshToken:text('refresh_token'),idToken:text('id_token'),accessTokenExpiresAt:timestamp('access_token_expires_at',{withTimezone:true}),refreshTokenExpiresAt:timestamp('refresh_token_expires_at',{withTimezone:true}),scope:text(),password:text(),createdAt:time('created_at'),updatedAt:time('updated_at')});
export const verification=pgTable('verification',{id:text().primaryKey(),identifier:text().notNull(),value:text().notNull(),expiresAt:timestamp('expires_at',{withTimezone:true}).notNull(),createdAt:time('created_at'),updatedAt:time('updated_at')});
export const workspaces=pgTable('workspace',{id:uuid().primaryKey().defaultRandom(),name:text().notNull(),createdAt:time('created_at')});
export const members=pgTable('member',{workspaceId:uuid('workspace_id').notNull().references(()=>workspaces.id,{onDelete:'cascade'}),userId:text('user_id').notNull().references(()=>user.id,{onDelete:'cascade'}),role:text().$type<'owner'|'editor'|'viewer'>().notNull()},t=>[primaryKey({columns:[t.workspaceId,t.userId]})]);
export const projects=pgTable('project',{id:uuid().primaryKey().defaultRandom(),workspaceId:uuid('workspace_id').notNull().references(()=>workspaces.id),name:text().notNull(),description:text().notNull().default(''),createdAt:time('created_at')},t=>[index('project_workspace').on(t.workspaceId)]);
export const datasets=pgTable('dataset',{id:uuid().primaryKey().defaultRandom(),projectId:uuid('project_id').notNull().references(()=>projects.id),name:text().notNull(),role:text().notNull(),fields:jsonb().$type<Field[]>().notNull(),keyField:text('key_field'),titleField:text('title_field').notNull(),schemaVersion:integer('schema_version').notNull().default(1)},t=>[index('dataset_project').on(t.projectId)]);
export const records=pgTable('record',{id:uuid().primaryKey().defaultRandom(),datasetId:uuid('dataset_id').notNull().references(()=>datasets.id),values:jsonb().$type<Record<string,Value>>().notNull(),externalKey:text('external_key'),version:integer().notNull().default(1),source:jsonb().$type<DataRecord['source']>().notNull(),updatedAt:time('updated_at'),updatedBy:text('updated_by').notNull().references(()=>user.id)},t=>[index('record_dataset').on(t.datasetId),uniqueIndex('record_external_key').on(t.datasetId,t.externalKey)]);
export const links=pgTable('record_link',{recordId:uuid('record_id').notNull().references(()=>records.id),fieldId:text('field_id').notNull(),targetId:uuid('target_id').notNull().references(()=>records.id)},t=>[primaryKey({columns:[t.recordId,t.fieldId]}),index('link_target').on(t.targetId)]);
export const revisions=pgTable('revision',{id:uuid().primaryKey().defaultRandom(),recordId:uuid('record_id').notNull().references(()=>records.id),authorId:text('author_id').notNull().references(()=>user.id),version:integer().notNull(),before:jsonb().$type<Record<string,Value>|null>(),after:jsonb().$type<Record<string,Value>>().notNull(),createdAt:time('created_at')},t=>[index('revision_record').on(t.recordId)]);
export const imports=pgTable('import_batch',{id:uuid().primaryKey().defaultRandom(),workspaceId:uuid('workspace_id').notNull().references(()=>workspaces.id),authorId:text('author_id').notNull().references(()=>user.id),diagnosis:jsonb().$type<Diagnosis>(),mapping:jsonb().$type<Confirmation>(),publishedProjectId:uuid('published_project_id').references(()=>projects.id),requestHash:text('request_hash'),createdAt:time('created_at')},t=>[index('import_workspace').on(t.workspaceId)]);
